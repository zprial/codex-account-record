{
  "name": "@codex-account-record/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"Shared 包测试尚未实现\""
  }
}
